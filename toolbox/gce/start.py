import logging
import sys


def run(repo_path: str, repo_name: str, config: dict):
    logging.error('Local VM start is not yet implemented. Please, create a VM of the same distro manually.')
    sys.exit(1)
